<!DOCTYPE html>
<html>
  <body>
    <head>
    <link rel="stylesheet" type="text/css" href="<%=url('./public/stylesheet.css')%>">
    <title>Bookmark Manager</title>
    </head>

    <body>
      <% if flash[:notice]%>
        <div id='notice'> <%= flash[:notice] %></div>
      <%end%>
      <% if current_user %>
        Welcome <%= current_user.name %>
      <% end %>
      <%= yield %>
    </body>
  </html>
