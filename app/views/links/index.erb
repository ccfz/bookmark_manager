

<h1> Links </h1>
p current_user
<ul id='links'>
  <% @links.each do |link| %>
    <li>
      Title: <%= link.title %>
      URL:   <%= link.url %>
      Tag: <%= link.tag.map(&:name) %>
    </li>
  <% end %>
</ul>